export interface Profile {
  id: number;

  email: string;
  firstName: string;
  lastName: string;
}

export interface ProfileForm {
  id: number;

  email: string;
  firstName: string;
  lastName: string;
}
